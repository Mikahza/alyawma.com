## What this PR does

<!-- One responsibility, in one sentence. If the sentence needs an "and", split the PR. -->

## Why

<!-- The problem, not the code. What was wrong or missing before this change? -->

## Proof it works

<!-- Required. A screenshot, a URL, or the passing test that demonstrates the change.
     "The CI is green" is not proof that a feature does what it claims. -->

## Shared environment

<!-- Delete this section if the change does not touch the server.
     Otherwise: what does it change on the host, and was it approved beforehand?
     This application shares a host with an unrelated production workload. -->

## Checklist

- [ ] This PR does one thing
- [ ] Commit messages follow Conventional Commits
- [ ] `composer ci:check` passes locally
- [ ] No secret, key or credential is added to a tracked file — this repository is public
